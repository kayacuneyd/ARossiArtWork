name: 🚀 Deploy Artist Portfolio to Hostinger

on:
  push:
    branches:
      - master
  workflow_dispatch: # Manuel trigger için

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🚀 Deploy via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ftp.arossiartwork.com
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          server-dir: /public_html/
          exclude: |
            **/.git*
            **/.git*/**
            **/.github/**
            .github/
            .gitignore
            .DS_Store
            .idea/
            .vscode/
            README.md
            DEPLOYMENT.md
            QUICKSTART.md
            schema.sql
            admin/setup.php
            .env.example
            composer.json
            composer.lock
            package.json
            package-lock.json
            node_modules/
            
      - name: ✅ Deployment Complete
        run: |
          echo "🎉 Deployment successful!"
          echo "🌐 Site: https://arossiartwork.com"
          echo "🔐 Admin: https://arossiartwork.com/admin/login.php"